# ═══════════════════════════════════════════════════════════
# UPDATELITE ARCH EDITION - CONFIGURATION
# ═══════════════════════════════════════════════════════════
# Location: ~/.config/updatelite/config
# Format: KEY=value (one per line)
# ═══════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────
# MODULES
# Enable or disable update modules (true/false)
# ─────────────────────────────────────────────────────────────

# System packages via pacman
ENABLE_PACMAN=true

# AUR packages via paru/yay/shelly
ENABLE_AUR=true

# Flatpak applications
ENABLE_FLATPAK=true

# Docker images (disabled by default)
ENABLE_DOCKER=false

# Firmware updates via fwupd
ENABLE_FIRMWARE=true

# ─────────────────────────────────────────────────────────────
# AUR SETTINGS
# ─────────────────────────────────────────────────────────────

# Preferred AUR helper: paru, yay, shelly, or auto (detect)
# auto prefers shelly, then paru, then yay (shelly is the CachyOS default since 2026)
AUR_HELPER=auto

# Packages to skip during AUR updates (space-separated)
# Example: AUR_SKIP_PACKAGES=nvidia-dkms-git linux-cachyos
AUR_SKIP_PACKAGES=

# ─────────────────────────────────────────────────────────────
# CLEANUP SETTINGS
# ─────────────────────────────────────────────────────────────

# Remove orphan packages
CLEANUP_ORPHANS=true

# Clean package cache (keep last 2 versions)
CLEANUP_CACHE=true

# Vacuum systemd journal
CLEANUP_JOURNAL=true

# Keep journal entries for this many days
JOURNAL_VACUUM_DAYS=7

# ─────────────────────────────────────────────────────────────
# LOGGING
# ─────────────────────────────────────────────────────────────

# Enable logging to file
ENABLE_LOGGING=false

# Log directory
LOG_DIR=$HOME/logs/updatelite

# ─────────────────────────────────────────────────────────────
# INTERFACE
# ─────────────────────────────────────────────────────────────

# Show motivational phrases at the end
ENABLE_PHRASES=true

# Enable colored output
ENABLE_COLORS=true

# ─────────────────────────────────────────────────────────────
# REBOOT DETECTION
# ─────────────────────────────────────────────────────────────

# Packages that require a reboot when updated (space-separated)
# When these are updated, you'll be notified to reboot
CRITICAL_PACKAGES=linux systemd glibc gcc-libs linux-firmware mesa nvidia fwupd

# ─────────────────────────────────────────────────────────────
# DEPENDENCY HOLDS
# ─────────────────────────────────────────────────────────────

# Hold back packages whose pinned dependency version does not match the
# installed one ("package:dependency" pairs, space-separated). Useful for
# kernel+driver bundles that pin an exact driver version.
# Example: DEP_MISMATCH_HOLDS=my-kernel-nvidia-open:nvidia-utils
DEP_MISMATCH_HOLDS=

# -----------------------------------------------------------------
# CONFLICT RESOLUTION
# -----------------------------------------------------------------

# Some upstream updates merge one package into another: the incoming package
# declares both Replaces and Provides for the installed one. pacman applies
# that rename silently once the old name is gone from every repo, but it asks
# for confirmation while the old name still exists somewhere (a CachyOS
# rebuild shadowing an Arch package, for instance) - and the unattended
# --noconfirm answers no, aborting the whole upgrade.
#
# With this enabled, updateLITE verifies the Replaces/Provides metadata of the
# incoming package and only then removes the superseded one and retries.
# Anything that is not a verified rename is left to you, and packages listed
# in CRITICAL_PACKAGES are never touched.
PACMAN_AUTO_RESOLVE_CONFLICTS=true
